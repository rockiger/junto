import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => {
    return {
        ul: {
            listStyleType: 'none',
            paddingLeft: 0,
        },
    }
})
