import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => {
    return {
        mydrive: {
            listStyleType: 'none',
            marginRight: '.5rem',
            paddingLeft: 0,
        },
    }
})
